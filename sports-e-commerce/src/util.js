// import React from 'react'


// function finder(cart , item ){
//     return state.cart.dfindrr
// }

// export default util

export const checkItem = (array, id) => {
    return array.find((item) => item.id === id);
  };
